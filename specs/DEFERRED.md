# Deferred Ideas Backlog

Every time a spec, clarify, or plan session explicitly defers something rather
than rejecting it, it gets logged here — so "later" has a fixed address
instead of living only inside one feature's spec file.

| Deferred in | Idea | Why deferred | Where it's protected |
|---|---|---|---|
| ~~`001-core-loop` (clarify Q1)~~ **RESOLVED by `006-race-visualizer`** | Live-paced / broadcast-style contest presentation (watch the race happen, not just see the result) | First slice only needs to prove the prepare→contest loop and its legibility; live presentation is a substantial feature in its own right | Constitution Principle IV (Spectation-First) — **now satisfied**, not just respected. `006-race-visualizer` shipped the watched ~20-second animated race this entry was holding a place for. |
| `001-core-loop` (clarify Q2) | Exact numeric attribution of outcome to specific build changes (vs. qualitative comparison) | Qualitative attribution is enough to prove legibility for this slice; numeric decomposition is real modeling work | Constitution Principle III (Transparency & Legibility) — numeric depth can be added later without violating the principle, since qualitative attribution already satisfies it |
| `001-core-loop` (clarify Q5) | Build-testing / retesting access (was User Story 3 + FR-008, fully removed from this spec) | Owner decided this should be designed as part of a later feature about encounters/events that change the build (e.g., a "test day"-style encounter), not partially built into the core loop slice | Constitution Principle V (Build Testing Access as Core, Not Bolt-On) — non-negotiable, must be satisfied by a dedicated later feature |
| Design conversation, pre-`002` | Weight as a soft, advisory constraint layered on top of the slot system (e.g., "this car feels heavier") | Multiple design personas converged that weight is real-world-legible in a way an abstract second currency isn't, but it should not become a second hard budget optimized in parallel with slots — needs its own design pass before it's real | Not yet protected by a constitution principle; tracked here so it isn't lost. Revisit once the slot system (`002`) has shipped and been played |
| Design conversation, pre-`002` | Item synergy / combination effects between held items (e.g., a tyre that's better paired with an engine item) | This is how team identity is meant to express itself once slot count is flat for everyone — but it's real content-design work, not part of proving the slot mechanic itself | Tracked here; `003-item-pool-draft` takes the first step with one tag-based buff item, but richer/multi-axis synergy remains deferred |
| `002-item-slots` (clarify Q2) | The run/encounter structure: how many encounters make up a run, encounter types (shop, reward, PvP, PvE) determining item quantity/character, and the two-layer "which encounter, then what to pick" choice | Exposed when a question about "how many rounds before a contest" had no real system to answer from yet — this is substantial, its own feature's worth of design, not something to solve inside the item-slot mechanic | Written up in `specs/vision.md` ("Run structure / encounter system"); `002-item-slots` uses a simple placeholder offer sequence specifically so it isn't blocked on this |
| ~~`003-item-pool-draft` (clarify Q3)~~ **PARTIALLY RESOLVED by `005-lap-tick-simulation`** | Lap-based, per-tick item effects with per-item cooldowns (Bazaar-style time/cooldown resolution) — a race as a discrete, scaling number of laps, where every item effect applies each lap/tick instead of once, gated by its own cooldown | Would replace the core contest-resolution architecture from `001-core-loop`/`002-item-slots` (a single computed time delta, resolved once) with a per-lap simulation loop — real architecture work, not a fit for a feature scoped to pool content + draft weighting | `005-lap-tick-simulation` shipped the per-lap/cooldown/flat-vs-stacking mechanism in full, using a **fixed** `LAP_COUNT` (10). The "scaling number of laps" half of this entry remains open — still tied to the undesigned run/encounter structure (`specs/vision.md`, row below) |
| `004-board-storage-ui` (clarify) | An item effect that grants additional Refresh (reroll) uses beyond the default one-per-round allowance (e.g., "an extra reroll at shops") | Raised while scoping the Refresh control's limit; a concrete instance of item-driven modifiers to a game-flow control, not just to race outcome — real content/balance work, not needed to prove the board/storage/refresh mechanic itself | Tracked here; `004-board-storage-ui`'s Refresh Allowance (data-model) is a per-round counter by design, deliberately not hard-coded to exactly 1 everywhere, so this can be layered on later without a rework |
| `006-race-visualizer` (scoping) | A skip/fast-forward control for the race animation | Considered during scoping alongside the 20-second watch duration, but not asked for and adds scope beyond what's needed to satisfy Constitution Principle IV for the first time | Tracked here; if added later, must only affect presentation pacing, never reopen or alter any already-computed outcome/gap value (Constitution Principle I) |

## Considered and rejected (not deferred — decided against)

For completeness, not everything raised in design conversations gets built later. These were considered and explicitly rejected, with reasoning, so nobody re-litigates them from scratch:

- **Rigid part-type slots** (fixed "engine slot," "aero slot," etc.) — rejected by the owner as too rigid; a plain slot count reads just as clearly to a spectator without forcing every item into a category.
- **Spatial/zone-based chassis diagram** (Backpack Battles-style physical packing) — even its own original proposer agreed this game's fun is the manager decision, not the geometry; the visual/UI cost isn't justified here.
- **Slot count varying by team identity** (e.g., a Performance team simply gets more total slots) — creates a strictly dominant strategy when every item lives in one undifferentiated pool (more slots is always better, unlike Hearthstone Battlegrounds' hero powers, which differ in kind, not just in scalar advantage). Identity belongs in the items and their synergies, not in container size.
- **Named capacity pools tied to identity** (e.g., "Chassis" vs. "Team" slots) — explored seriously, three of four design personas endorsed it, but ultimately set aside in favor of the simpler flat-slots-plus-item-synergy approach once the slot-count-variance idea was ruled out for the same underlying reason (it re-introduces a category system the owner already rejected once).
- **Homologation lock-in** (can't swap a build between prepare and contest once "locked") — was proposed as a companion to a continuous-budget system; the motorsport-engineer persona who proposed it agreed it doesn't solve a real problem once the constraint is a discrete slot count instead.

**Convention**: add a row whenever a spec/clarify/plan session defers something explicitly (as opposed to deciding against it). Note which feature deferred it, why, and — if one exists — which constitutional principle guarantees it gets picked back up. When a later feature ships and resolves an entry, strike through its "Deferred in" cell, prefix it with **RESOLVED by** or **PARTIALLY RESOLVED by** and the resolving feature, and update the "Where it's protected" cell to say what actually shipped — don't delete the row, so the history of what was deferred and for how long stays visible.
